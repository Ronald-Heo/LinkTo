'use strict';

import angular from 'angular';
// import intro from '../templates/intro.html';

const templates = angular.module('templates', []);
// require('../../../base/templates/**/*.html', {mode: 'expand'});
// require('../templates/**/*.html', {mode: 'expand'});

export default templates;
