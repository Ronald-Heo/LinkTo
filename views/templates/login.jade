extends ../layout

block content
    h1= title

    form
        p 아이디
        input(type='text' class='form-control')
        p 패스워드
        input(type='password' class='form-control')
        p
        button(class='btn btn-primary form-control') 로그인
